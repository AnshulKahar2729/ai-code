import React, { FC } from "react";

interface ChatProps {
//   messages: string[];
}
export const Chat: FC<ChatProps> = ({}) => {
  return (
    <div>
      <h1>Chat component </h1>
    </div>
  );
};
